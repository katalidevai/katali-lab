<Window x:Class="KataliLabGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Katali Lab Chat"
        Width="1100" Height="740"
        MinWidth="960" MinHeight="640"
        Background="{StaticResource BgDeep}"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, sans-serif">

    <Window.Resources>
        <Style x:Key="PlaceholderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Margin" Value="14,12,14,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ===== Top bar ===== -->
        <Border Grid.Row="0"
                Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderSubtle}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="8" Height="8" CornerRadius="4"
                            Background="{StaticResource Accent}"
                            Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Katali Lab"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ComboBox x:Name="ModelCombo"
                          Grid.Column="2"
                          Style="{StaticResource DarkCombo}"
                          VerticalAlignment="Center"
                          MinWidth="240"
                          DisplayMemberPath="Label"
                          SelectionChanged="ModelCombo_SelectionChanged"
                          ToolTip="Model from C:\models, recents, or Browse…"/>

                <Button x:Name="BrowseModelBtn"
                        Grid.Column="4"
                        Content="Browse…"
                        Style="{StaticResource GhostButton}"
                        Click="BrowseModel_Click"
                        ToolTip="Pick a .gguf from any drive. Shift+Browse… = pick a multi-shard folder."/>

                <Button x:Name="BrowseFolderBtn"
                        Grid.Column="6"
                        Content="Folder…"
                        Style="{StaticResource GhostButton}"
                        Click="BrowseFolder_Click"
                        Padding="10,8"
                        ToolTip="Pick a folder for multi-shard GGUF models (any drive)."/>

                <TextBlock Grid.Column="8"
                           Text="Max tokens"
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0"/>

                <TextBox x:Name="MaxTokensBox"
                         Grid.Column="10"
                         Text="512"
                         Width="72"
                         VerticalAlignment="Center"
                         Background="{StaticResource BgInput}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BorderSubtle}"
                         BorderThickness="1"
                         Padding="8,6"
                         FontSize="13"
                         CaretBrush="{StaticResource Accent}"
                         ToolTip="Maximum tokens to generate"/>

                <Button x:Name="RefreshModelsBtn"
                        Grid.Column="12"
                        Content="Refresh models"
                        Style="{StaticResource GhostButton}"
                        Click="RefreshModels_Click"
                        ToolTip="Rescan C:\models (keeps browsed/recent entries)"/>

                <Button x:Name="ClearChatBtn"
                        Grid.Column="14"
                        Content="Clear chat"
                        Style="{StaticResource GhostButton}"
                        Click="ClearChat_Click"
                        ToolTip="Clear the conversation transcript"/>
            </Grid>
        </Border>

        <!-- ===== Chat transcript ===== -->
        <Border Grid.Row="1" Background="{StaticResource BgDeep}">
            <ScrollViewer x:Name="ChatScroll"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="16,12">
                <StackPanel x:Name="ChatPanel">
                    <TextBlock x:Name="EmptyHint"
                               Text="Select a model and send a message to begin."
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               Margin="0,80,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ===== Input area ===== -->
        <Border Grid.Row="2"
                Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderSubtle}"
                BorderThickness="0,1,0,0"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{StaticResource BgInput}"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="2">
                    <Grid>
                        <TextBox x:Name="PromptBox"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="56"
                                 MaxHeight="160"
                                 VerticalScrollBarVisibility="Auto"
                                 Background="Transparent"
                                 Foreground="{StaticResource TextPrimary}"
                                 BorderThickness="0"
                                 Padding="12,10"
                                 FontSize="14"
                                 CaretBrush="{StaticResource Accent}"
                                 VerticalContentAlignment="Top"
                                 PreviewKeyDown="PromptBox_PreviewKeyDown"
                                 TextChanged="PromptBox_TextChanged"/>
                        <TextBlock x:Name="PromptPlaceholder"
                                   Text="Message Katali Lab…"
                                   Style="{StaticResource PlaceholderStyle}"/>
                    </Grid>
                </Border>

                <!-- Stop/Send stretch to match input MinHeight 56 -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Stretch">
                    <Button x:Name="StopBtn"
                            Content="Stop"
                            Style="{StaticResource StopButton}"
                            IsEnabled="False"
                            Click="Stop_Click"
                            Margin="0,0,8,0"
                            MinWidth="72"
                            MinHeight="56"
                            VerticalAlignment="Stretch"
                            ToolTip="Stop generation"/>
                    <Button x:Name="SendBtn"
                            Content="Send"
                            Style="{StaticResource PrimaryButton}"
                            Click="Send_Click"
                            MinWidth="80"
                            MinHeight="56"
                            VerticalAlignment="Stretch"
                            ToolTip="Send message (Enter)"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== Status / timer strip (under input) ===== -->
        <Border Grid.Row="3"
                Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderSubtle}"
                BorderThickness="0,1,0,0"
                Padding="16,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Live: "Generating… 12.3s"  |  Done: "Last reply: 48.2s" -->
                <TextBlock x:Name="StatusText"
                           Text="idle"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="12"
                           VerticalAlignment="Center"/>
                <!-- tok/s from CLI stderr when available -->
                <TextBlock x:Name="SpeedText"
                           Grid.Column="1"
                           Text=""
                           Foreground="{StaticResource Accent}"
                           FontSize="12"
                           FontFamily="Consolas, Cascadia Mono, monospace"
                           VerticalAlignment="Center"
                           ToolTip="Decode speed from katali-lab (when reported)"/>
            </Grid>
        </Border>
    </Grid>
</Window>
